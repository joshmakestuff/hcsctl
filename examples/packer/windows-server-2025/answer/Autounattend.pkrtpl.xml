<?xml version="1.0" encoding="utf-8"?>
<!--
  Answer file for an unattended Windows Server 2025 install under Packer.

  Rendered by templatefile() from server-2025.pkr.hcl. Two values are substituted:
  ${image_name} and ${admin_password}. The password is written into XML, so a value that
  contains & < > " or ' will break the file. Use a simple value.

  WinRM is started in the SPECIALIZE pass, not from FirstLogonCommands. Measured on Server
  Core: a first-logon command is registered in setupact.log and never runs, because the
  Explorer first-logon path does not exist on that edition. A specialize-pass synchronous
  command runs as SYSTEM on every edition.

  The listener is created directly instead of by `winrm quickconfig`. quickconfig refuses
  when a network adapter has the Public profile, which is the profile a machine has before
  OOBE sets the network location.

  Example scaffolding: replace with your own base-image process. Reused as-is from the
  workspace image templates; measured working on Server 2025 Core.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">

  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

      <DiskConfiguration>
        <WillShowUI>OnError</WillShowUI>
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>
          <CreatePartitions>
            <!-- EFI system partition. -->
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>EFI</Type>
              <Size>260</Size>
            </CreatePartition>
            <!-- Microsoft reserved partition. -->
            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>MSR</Type>
              <Size>128</Size>
            </CreatePartition>
            <!-- Windows. Takes the remaining space. -->
            <CreatePartition wcm:action="add">
              <Order>3</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>
          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Label>System</Label>
              <Format>FAT32</Format>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>2</Order>
              <PartitionID>2</PartitionID>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>3</Order>
              <PartitionID>3</PartitionID>
              <Label>Windows</Label>
              <Format>NTFS</Format>
              <Letter>C</Letter>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
      </DiskConfiguration>

      <ImageInstall>
        <OSImage>
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>${image_name}</Value>
            </MetaData>
          </InstallFrom>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>3</PartitionID>
          </InstallTo>
          <WillShowUI>OnError</WillShowUI>
        </OSImage>
      </ImageInstall>

      <UserData>
        <!-- An empty key skips the product key page. The edition comes from /IMAGE/NAME. -->
        <ProductKey>
          <Key></Key>
        </ProductKey>
        <AcceptEula>true</AcceptEula>
        <FullName>hcs-images</FullName>
        <Organization>hcs-images</Organization>
      </UserData>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <ComputerName>hcs-guest</ComputerName>
      <TimeZone>UTC</TimeZone>
    </component>

    <component name="Microsoft-Windows-Deployment" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <RunSynchronous>
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Set the WinRM service to start automatically</Description>
          <Path>cmd.exe /c sc config WinRM start= auto</Path>
          <WillReboot>Never</WillReboot>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Description>Start the WinRM service</Description>
          <Path>cmd.exe /c net start WinRM</Path>
          <WillReboot>Never</WillReboot>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>3</Order>
          <Description>Create the WinRM HTTP listener</Description>
          <Path>cmd.exe /c winrm create winrm/config/Listener?Address=*+Transport=HTTP</Path>
          <WillReboot>Never</WillReboot>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>4</Order>
          <Description>Let WinRM accept an unencrypted message body</Description>
          <!--
            Measured 2026-08-08. Without this, Packer answers a correct Administrator
            credential with a repeating 401 and the build waits until winrm_timeout.

            Packer's NTLM transport authenticates the request but does not encrypt the
            message body. A WinRM service with AllowUnencrypted=false rejects that at the
            HTTP layer, so the failure looks like a bad password and is not one. A host-side
            probe with the same credential reproduced the 401, and the connection succeeded
            the moment this value became true.

            The build runs on an internal switch and the guest is a test fixture, so a
            plaintext body during the build is acceptable.
          -->
          <Path>cmd.exe /c winrm set winrm/config/service @{AllowUnencrypted=&quot;true&quot;}</Path>
          <WillReboot>Never</WillReboot>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>5</Order>
          <Description>Open TCP 5985 for WinRM on every profile</Description>
          <Path>cmd.exe /c netsh advfirewall firewall add rule name=&quot;WinRM-HTTP-In-TCP-5985&quot; dir=in action=allow protocol=TCP localport=5985 profile=any</Path>
          <WillReboot>Never</WillReboot>
        </RunSynchronousCommand>
      </RunSynchronous>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-International-Core" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <UserAccounts>
        <AdministratorPassword>
          <Value>${admin_password}</Value>
          <PlainText>true</PlainText>
        </AdministratorPassword>
      </UserAccounts>
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideLocalAccountScreen>true</HideLocalAccountScreen>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <!-- Work sets the Private network profile, which the WinRM firewall rule does not
             depend on. It is set because a Public profile blocks other guest services. -->
        <NetworkLocation>Work</NetworkLocation>
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>
    </component>
  </settings>

</unattend>
